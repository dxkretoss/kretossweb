
> .hero {
    background-color: var(--_color---theme-colors--background-color);
    background-image: 
url("https://cdn.prod.website-files.com/6996a337655d586ffe288775/6996a337655d586ffe2887f7_Frame%202147239836.webp");
    background-position: 50%;
    background-size: cover;
    flex-flow: column;
    display: flex;
    position: relative;
  }
  
> .hero-top-content {
    margin-top: var(--_spacing---spacing--space-regular-xxl);
    flex-flow: column;
    justify-content: center;
    align-items: center;
    display: flex;
  }
  
> .hero-icon-rating {
    grid-column-gap: var(--_spacing---gaps--gap-small-xxl);
    grid-row-gap: var(--_spacing---gaps--gap-small-xxl);
    justify-content: flex-start;
    align-items: center;
    display: flex;
  }
  
> .hero-icon-box {
    justify-content: flex-start;
    align-items: center;
    display: flex;
  }
  
> .hero-meta-box-icon {
    height: 17px;
  }
  
> .hero-rating-text {
    grid-column-gap: var(--_spacing---gaps--gap-tiny-xxl);
    grid-row-gap: var(--_spacing---gaps--gap-tiny-xxl);
    color: #d0d0d0;
    font-size: 18px;
    line-height: var(--line-height--height-flat);
    justify-content: flex-start;
    align-items: center;
    display: flex;
  }
  
> .hero-rating-text.contact-rating {
    color: #3a3737;
  }
  
> .hero-icon-text {
    grid-column-gap: var(--_spacing---gaps--gap-tiny-xxl);
    grid-row-gap: var(--_spacing---gaps--gap-tiny-xxl);
    justify-content: flex-start;
    align-items: center;
    display: flex;
    overflow: hidden;
  }
  
> .single-tag-item {
    color: var(--_color---base-colors--white);
    font-size: 16px;
    line-height: var(--line-height--height-medium);
    font-weight: var(--font-weight--font-medium);
  }
  
> .single-tag-item._01 {
    z-index: 1;
    position: relative;
  }
  
> .single-tag-item._02 {
    z-index: 2;
    opacity: 1;
    position: absolute;
    inset: 100% auto auto 0%;
  }
  
> .single-tag-item._03 {
    z-index: 3;
    opacity: 1;
    position: absolute;
    inset: 100% auto auto 0%;
  }
  
> .single-tag-item._04 {
    z-index: 4;
    opacity: 1;
    display: block;
    position: absolute;
    inset: 100% auto auto 0%;
  }
  
> .hero-title-box {
    text-align: center;
    flex-flow: column;
    justify-content: center;
    align-items: center;
    display: flex;
  
    position: relative;
  }
  
  
> .hero-title {
    color: var(--_color---base-colors--white);
    font-size: 80px;
    line-height: var(--line-height--height-large);
    font-weight: var(--font-weight--font-bold);
    justify-content: center;
    align-items: center;
    display: flex;
  }
  
> .hero-title._03 {
    font-size: 64px;
  }
  
> .hero-title.banner {
    grid-column-gap: 10px;
    grid-row-gap: 10px;
    font-size: 64px;
    text-transform: uppercase;
    display: flex;
  }
  
> .hero-title.banner-02 {
    grid-column-gap: 9px;
    grid-row-gap: 9px;
    font-size: px;
    text-transform: uppercase;
    position: relative;
  }
  
> .hero-text-span {
    color: #bd73e8;
  }
  
> .hero-text-span._03 {
    font-family: var(--_font---font-two);
    color: var(--_color---base-colors--white);
  }
  
> .hero-text-span.banner-03 {
    color: #ff8f27;
    font-size: 64px;
    text-transform: uppercase;
  }
  
> .hero-icon-wrapper {
    grid-column-gap: var(--_spacing---gaps--gap-regular-xxl);
    grid-row-gap: var(--_spacing---gaps--gap-regular-xxl);
    justify-content: center;
    align-items: center;
    display: flex;
  }
  
> .hero-icon-title-wrapper {
    grid-column-gap: var(--_spacing---gaps--gap-small-xxl);
    grid-row-gap: var(--_spacing---gaps--gap-small-xxl);
    flex-flow: column;
    justify-content: flex-start;
    align-items: center;
    display: flex;
  }
  
> .hero-shap-wrapper {
    flex-flow: column;
    justify-content: flex-end;
    align-items: flex-start;
    display: flex;
    position: relative;
    transform: rotate(2deg);
  }
  
> .hero-shap-wrapper.right-shape {
    justify-content: flex-end;
    align-items: flex-end;
    margin-top: 10px;
    position: relative;
    transform: none;
  }
  
> .hero-shap-wrapper._01 {
    margin-top: 10px;
    transform: none;
  }
  
> .hero-shape-box {
    padding: var(--_spacing---spacing--space-small-xl) var(--_spacing---spacing--space-small-xxl);
    grid-column-gap: var(--_spacing---gaps--gap-tiny);
    grid-row-gap: var(--_spacing---gaps--gap-tiny);
    border-radius: var(--border-radius--radius-tiny);
    background-color: #bd73e8;
    justify-content: center;
    align-items: center;
    width: 170px;
    height: 48px;
    display: flex;
  }
  
> .hero-shape-box.web {
    background-color: #A9BF15;
  }
  
> .hero-shape-box.right-shape {
    padding-right: var(--_spacing---spacing--space-regular);
    padding-left: var(--_spacing---spacing--space-regular);
    background-color: #ff8f27;
    justify-content: flex-start;
    align-items: center;
    width: auto;
    height: 48px;
    transform: rotate(-2.22deg);
  }
  
> .hero-shape-box._01 {
    padding-right: var(--_spacing---spacing--space-regular);
    padding-left: var(--_spacing---spacing--space-regular);
    width: auto;
    transform: rotate(2.22deg);
  }
  
> .hero-shape-text {
    color: var(--_color---theme-colors--background-color);
    font-size: 20px;
    line-height: var(--line-height--height-flat);
    font-weight: var(--font-weight--font-medium);
  }
  
> .hero-shaper-icon {
    width: 24px;
    height: 24px;
  }
  
> .hero-arrow-box {
    position: absolute;
    inset: -52% -21% auto auto;
  }
  
> .hero-arrow-box._03 {
    inset: 95% -22% auto auto;
  }
  
> .hero-arrow-box.right-arrow {
    inset: -53% auto auto -17%;
  }
  
> .hero-arrow-box.hero-arrow-box._02 {
    inset: 92% auto auto -12%;
  }
  
> .hero-text-button {
    grid-column-gap: var(--_spacing---gaps--gap-medium-xxl);
    grid-row-gap: var(--_spacing---gaps--gap-medium-xxl);
    text-align: center;
    flex-flow: column;
    justify-content: flex-start;
    align-items: center;
    max-width: 900px;
    display: flex;
  }
  
> .hero-text {
    color: #fffc;
    font-size: 22px;
    line-height: var(--line-height--height-extra);
    font-weight: var(--font-weight--font-regular);
  }
  
> .hero-content-wrapper {
    border-left: 0 solid #ffffff29;
    border-right: 0 solid #ffffff29;
    flex-flow: column;
    padding-top: 130px;
    padding-bottom: 150px;
    display: flex;
  }
  
  .step {
    padding: var(--_spacing---spacing--space-bold-xl) var(--_spacing---spacing--space-regular-xxl);
    background-color: var(--_color---theme-colors--background-color);
    background-image: 
url("https://cdn.prod.website-files.com/6996a337655d586ffe288775/6996a337655d586ffe28881a_Frame%202147228747.webp");
    background-position: 50%;
    background-size: cover;
    width: 100%;
    height: 1200px;
    position: relative;
    top: -2px;
  }
  
> .hero-link-block {
    z-index: 3;
    grid-column-gap: var(--_spacing---gaps--gap-regular-xxl);
    grid-row-gap: var(--_spacing---gaps--gap-regular-xxl);
    justify-content: space-between;
    align-items: center;
    max-width: 683px;
    margin-left: auto;
    margin-right: auto;
    display: flex;
    position: relative;
  }
  
> .hero-link-bar {
    padding: var(--_spacing---spacing--space-small) var(--_spacing---spacing--space-medium-xl);
    grid-column-gap: 48px;
    grid-row-gap: 48px;
    border-radius: var(--border-radius--radius-tiny-xl);
    background-color: #0a0a0a;
    justify-content: space-between;
    align-items: center;
    display: flex;
    box-shadow: inset 0 4px 4px #32a1f466;
  }
  
> .hero-link {
    color: var(--_color---base-colors--white);
    font-size: 16px;
    line-height: var(--line-height--height-flat);
    text-decoration: none;
    position: relative;
    overflow: hidden;
  }
  
> .hero-link.w--current {
    color: #3eb9f5;
  }
  
  .header {
    background-color: var(--_color---theme-colors--background-color);
    border-bottom: 1px solid #ffffff1a;
  }
  
  .container-full-width {
    width: 100%;
    max-width: 1620px;
    padding-left: 20px;
    padding-right: 20px;
  }
  
  .container-full-width.hero-container {
    z-index: 4;
    position: relative;
  }
  
> .hero-title-box-two {
    align-items: center;
    display: flex;
    position: relative;
  }
  
> .hero-title-image-box {
    z-index: 5;
    justify-content: center;
    align-items: center;
    margin-top: -12px;
    display: flex;
    position: relative;
  }
  
> .hero-image-01 {
    z-index: 1;
    opacity: 0;
    width: 117px;
    position: relative;
    top: 0;
    left: -4px;
    transform: scale(1.3);
  }
  
> .hero-image-02 {
    opacity: 0;
    width: 117px;
    position: absolute;
    transform: scale(1.3)rotate(-3deg);
  }
  
> .hero-text-three {
    grid-column-gap: 40px;
    grid-row-gap: 40px;
    justify-content: center;
    align-items: center;
    margin-top: 0;
    display: flex;
    position: relative;
    overflow: visible;
  }
  
  .button-arrow-box-icon {
    justify-content: center;
    align-items: center;
    display: flex;
    position: relative;
    overflow: hidden;
  }
  
  .about-block {
    grid-column-gap: 20px;
> .hero-star-wrapper {
    grid-column-gap: 7px;
    grid-row-gap: 7px;
    align-items: center;
    display: flex;
    position: relative;
  }
  
> .single-review-star {
    width: 17px;
    height: 17px;
  }
  
> .hero-meta-icon-box {
    background-image: 
url("https://cdn.prod.website-files.com/6996a337655d586ffe288775/69acfc6d68ffa3b108959e63_Frame%202147227831.png");
    background-position: 0 0;
    background-size: 100% 100%;
    justify-content: center;
    align-items: center;
    width: 28px;
    min-width: 28px;
    height: 28px;
    display: flex;
  }
  
> .hero-meta-tag-box {
    position: relative;
    overflow: hidden;
  }
  
> .trust-score {
    z-index: 3;
    opacity: 0;
    color: var(--_color---base-colors--white);
    font-size: 16px;
    position: absolute;
  }
  
> .trust-score._02 {
    color: var(--_color---theme-colors--background-color);
  }
  
> .hero-left-meta-icon {
    height: 17px;
  }
  
> .hero-left-meta-icon._01 {
    z-index: 2;
    position: relative;
  }
  
> .hero-left-meta-icon._01.icon {
    height: 28px;
  }
  
> .hero-left-meta-logo {
    height: 17px;
  }
  
> .hero-left-meta-logo._02 {
    margin-left: -3px;
  }
  
> .hero-left-meta-logo._02.icon {
    height: 28px;
  }
  
> .hero-top-title {
    display: flex;
  }
  
> .hero-bbottom-ttitle {
    color: var(--_color---base-colors--white);
    font-size: 80px;
    line-height: var(--line-height--height-large);
    font-weight: var(--font-weight--font-bold);
    justify-content: center;
    align-items: center;
    display: flex;
  }
  
> .hero-bbottom-ttitle.banner-04 {
    font-size: 64px;
    text-transform: uppercase;
    font-style: italic;
  }
  
> .hero-brand-ttitle {
    grid-column-gap: 12px;
    grid-row-gap: 12px;
    display: flex;
  }
  
  .project-single-item {
    padding: var(--_spacing---gaps--gap-regular);
    grid-column-gap: var(--_spacing---gaps--gap-regular);
    grid-row-gap: var(--_spacing---gaps--gap-regular);
    border-radius: var(--border-radius--radius-tiny-xxl);
    transform-origin: 50% 0;
    background-color: #c080ed;
    justify-content: space-between;
    align-items: stretch;
    height: 100%;
    display: flex;
    position: sticky;
    top: 20px;
    transform: perspective(2000px);
  }
> .single-review-box {
    grid-column-gap: 20px;
    grid-row-gap: 20px;
    flex: none;
    display: flex;
  }
  
  .review-slider-content {
    grid-column-gap: 20px;
    grid-row-gap: 20px;
    display: flex;
  }
  
  .contact-shape-block {
    z-index: 1;
    perspective: 1000px;
    width: 212px;
    height: 212px;
    position: absolute;
    inset: 0% auto auto 0%;
  }
> .hero-contaciner-border {
    z-index: 1;
    justify-content: center;
    width: 100%;
    height: 100%;
    padding-left: 20px;
    padding-right: 20px;
    display: flex;
    position: absolute;
    inset: 0% auto auto 0%;
  }
  
> .hero-border-box {
    z-index: 1;
    border-left: 1px solid #ffffff29;
    border-right: 1px solid #ffffff29;
    width: 100%;
    max-width: 1620px;
    height: 100%;
    position: relative;
  }
  
> .hero-border-shape {
    z-index: 2;
    object-position: 50% 100%;
    width: 100%;
    height: 166px;
    position: absolute;
    inset: auto auto 0% 0%;
  }
  
  .review-ssubtitle-title {
    grid-column-gap: var(--_spacing---gaps--gap-small-xl);
    grid-row-gap: var(--_spacing---gaps--gap-small-xl);
    flex-flow: column;
    align-items: center;
    display: flex;
  }
  
  .about-section-title {
    font-size: 36px;
    line-height: 140%;
    font-weight: var(--font-weight--font-semi-bold);


